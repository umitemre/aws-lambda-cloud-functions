service: aws-lambda-cloud-functions

frameworkVersion: '3'

plugins:
  - serverless-jetpack

provider:
    name: aws
    runtime: nodejs18.x
    region: eu-central-1

functions:
  api:
    handler: dist/lambda.handler
    events: []